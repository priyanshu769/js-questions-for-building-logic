// Question2: Given an array and an item, your function should return the index at which the item is present.

const itemsIndex = (array, item) => array.indexOf(item);

// console.log(itemsIndex([1, 23, 5, 6], 5));
