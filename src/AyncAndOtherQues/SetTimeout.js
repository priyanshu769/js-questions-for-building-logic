// Question: write a function which takes a message and a delay and print that message after the delay.

const delayPrint = () => {
  console.log("This my secret msg, which will get printed in 3 seconds.");
};

// setTimeout(delayPrint(), 3000);
